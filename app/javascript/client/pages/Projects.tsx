import React from "react"
import { Layout } from "../components/Layout";

export const Projects = () => {
    return (
        <Layout>
            <div>
                Project
            </div>
        </Layout>
    );
}