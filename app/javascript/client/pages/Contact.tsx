import React from "react"
import { Layout } from "../components/Layout";

export const Contact = () => {
    return (
        <Layout>
            <div>
                Contact Page
            </div>
        </Layout>
    );
}